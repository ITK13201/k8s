---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: calendarrabbit-secret
  namespace: calendarrabbit
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: calendarrabbit-secret
    creationPolicy: Owner
  data:
  - secretKey: CLAUDE_API_KEY
    remoteRef:
      key: calendarrabbit-secret
      property: CLAUDE_API_KEY
  - secretKey: DB_PASSWORD
    remoteRef:
      key: calendarrabbit-secret
      property: DB_PASSWORD
  - secretKey: DEEPSEEK_API_KEY
    remoteRef:
      key: calendarrabbit-secret
      property: DEEPSEEK_API_KEY
  - secretKey: SEARCH_API_KEY
    remoteRef:
      key: calendarrabbit-secret
      property: SEARCH_API_KEY
  - secretKey: GOOGLE_OAUTH_CLIENT_ID
    remoteRef:
      key: calendarrabbit-secret
      property: GOOGLE_OAUTH_CLIENT_ID
  - secretKey: GOOGLE_OAUTH_CLIENT_SECRET
    remoteRef:
      key: calendarrabbit-secret
      property: GOOGLE_OAUTH_CLIENT_SECRET
  - secretKey: GOOGLE_OAUTH_REDIRECT_URL
    remoteRef:
      key: calendarrabbit-secret
      property: GOOGLE_OAUTH_REDIRECT_URL
  - secretKey: GOOGLE_TOKEN_ENC_KEY
    remoteRef:
      key: calendarrabbit-secret
      property: GOOGLE_TOKEN_ENC_KEY
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: calendarrabbit-mysql-secret
  namespace: calendarrabbit
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: calendarrabbit-mysql-secret
    creationPolicy: Owner
  data:
  - secretKey: MYSQL_PASSWORD
    remoteRef:
      key: calendarrabbit-mysql-secret
      property: MYSQL_PASSWORD
  - secretKey: MYSQL_ROOT_PASSWORD
    remoteRef:
      key: calendarrabbit-mysql-secret
      property: MYSQL_ROOT_PASSWORD
