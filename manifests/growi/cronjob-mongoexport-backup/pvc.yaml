---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mongoexport-backup-pvc
  labels:
    app: growi
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: manual
  selector:
    matchLabels:
      target-app: growi-mongoexport-backup
