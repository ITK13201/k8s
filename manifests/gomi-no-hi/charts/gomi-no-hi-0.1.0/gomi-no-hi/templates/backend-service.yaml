apiVersion: v1
kind: Service
metadata:
  name: {{ include "gomi-no-hi.backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gomi-no-hi.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "gomi-no-hi.backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
