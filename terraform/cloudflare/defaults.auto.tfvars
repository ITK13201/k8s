# 非秘密・非個人のデフォルト（committed）。
# `*.auto.tfvars` は terraform が自動読込するため Makefile 側の指定は不要。
#
# cloudflare ワークスペースの変数はすべて秘密の認証情報または
# 個人情報（account_id / home_ip / さくらメール設定 / DKIM 等）であり、
# env.op の op:// 参照で供給する。ここに書くべき非秘密・非個人のデフォルトは現状ない。
#
# 注意: 個人情報（IP アドレス・ホスト名・メールアドレス等）はここに書かないこと。
